# dependencies / build output
node_modules/
artifacts/
cache/
typechain-types/
coverage/
coverage.json
dist/
.next/

# env + secrets
.env
.env.*
!.env.example

# preserved upstream clone metadata (local only)
payment-integrators/.git.bak/

# machine-local dev deployment addresses
backend/local-deployment.json

# admin panel — kept out of the public repo (internal tooling)
admin-panel/

# throwaway on-chain probe / snapshot scripts (never committed)
frontend/_*.mjs

# editor / os
.vscode/
.DS_Store
*.log
.claude/

PUSH.bat
